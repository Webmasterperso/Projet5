<!DOCTYPE html>
<html lang="fr">
    <head>
        <title>titre head</title>
    </head>
    <body>
        <p>essai affichage</p>
        <ul id="navigation">
            {% for user in users %}
                <li>username : {{ user.username|e }}</li>
            {%  else  %}
                Aucun lien n'a été trouvé.
            {% endfor %}
        </ul>

        <h1>Mes oeuvres</h1>
        {{ a_variable }}
        {{ users }}
    </body>
</html>